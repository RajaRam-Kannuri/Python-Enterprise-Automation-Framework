"""
Elements in this module are static. They must have static root and can only be presented once per page.
If this true, we can avoid declaring its root in the constructor.
Example:
some_modal = SomeModal()
"""
